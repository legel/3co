#hexagon_radius = 0.1
#hexagon_origin = 0.5

# a_x = hexagon_radius + hexagon_origin
# a_y = 0.0
# a_yaw = 0.0
# b_x = hexagon_radius / 2.0 + hexagon_origin
# b_y = -1 * (math.sqrt(3) / 2.0) * hexagon_radius
# b_yaw = 60.0
# c_x = -1 * hexagon_radius / 2.0 + hexagon_origin
# c_y = -1 * (math.sqrt(3) / 2.0) * hexagon_radius
# c_yaw = 120.0
# d_x = -1 * hexagon_radius + hexagon_origin
# d_y = 0.0
# d_yaw = 175.0
# e_x = -1 * hexagon_radius / 2.0 + hexagon_origin
# e_y = (math.sqrt(3) / 2.0) * hexagon_radius
# e_yaw = -60.0
# f_x = hexagon_radius / 2.0 + hexagon_origin
# f_y = (math.sqrt(3) / 2.0) * hexagon_radius
# f_yaw = -120.0

#z = 0.30